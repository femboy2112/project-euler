# Session Modes — the commands

## The mode

- **`/euler-mode`** — become Euler for the session: the voice (serene industry, honest
  delight, generosity — the one mode in the family with no contempt register), the Engine
  policies as standing law, the Ledger, the imprimatur gates. The user is the Correspondent.
  `/euler-mode off` restores the plain terminal.

## The standing arrangements

- **`/academy [cast]`** — the ensemble: every turn convenes the same correspondents
  (default: goldbach, lagrange, dalembert, daniel) for work or short in-voice marginalia that
  sharpen the plan before Euler acts. `/academy off` adjourns.
- **`/commentary [on|off]`** — the cast's in-voice code-comment track (on by default; `off`
  for shared/serious source).

## The flagship

- **`/opera <goal>`** — the Academy as a deterministic Workflow: Observe (daniel ∥ johann) →
  Conjecture (goldbach, briefed with ALL observations) → Attack & Verify (per conjecture,
  dalembert ∥ daniel via pipeline) → Demonstrate (lagrange, survivors only) → Publish
  (the-princess). Shared intel briefed forward; every claim lands Ledger-labeled; Euler's desk
  verifies before anything is presented.

## The one-shot beats

| Command | The move | The anchor |
|---|---|---|
| `/specimen <object>` | The observation pass: calibrate, compute in appetite, **dwell** (numbered observations before any conjecture), report patterns worthy of attention | E256 |
| `/basel <question>` | The full single-problem arc: numerics first → daring guess → the three-installment payment → demonstration attempt → publish | E41 |
| `/bridges <problem>` | The skeleton reduction: strip to the deciding invariant, rule feasible/infeasible before anyone builds | E53 |
| `/divergent <boundary>` | The audacity license: cross the proven domain in a sandbox, labeled, paid with multi-route consistency; GO/NO-GO | E247 |
| `/second-proof <claim> [N]` | N independent blind derivations; consensus only; splits surfaced as signal | Euler's re-proving habit |
| `/refute <claim>` | The 641 gate: structure theorem first, then hunt where the counterexample must live; SURVIVED (bounded) or REFUTED (tombstone) | E26/E134 |
| `/ledger [scope]` | The claims audit: re-grade everything load-bearing; hunt silent upgrades; demote loudly | E175's discipline |
| `/eloge [teaser]` | The session chronicle: what was Observed/Conjectured/Demonstrated/Refuted, false starts included, UNVERIFIED louder than it happened | Condorcet's Éloge |
| `/scriptorium <front>` | The fuss-swarm over a broad mechanical front; refuses non-fronts | The blind decade's scriptorium |
| `/masters <name\|problem>` | The masters' wing: apply another S-tier skillset in general form — saturated slate, chiliad count, long dwell, structural reconstitution, analytic legitimization, problem economy, rising sea, creation test, post-rigorous stage | Dossiers V–IX |

## The auto-discovered skills

- **`the-euler-method`** — the method itself, loaded automatically on hard math/algorithmic
  problems, pattern hunts, and conjecture-vs-proof situations. Runs plain when euler-mode is
  off, in voice when on. The mode is optional; the method is the point.
- **`the-masters`** — the masters' wing repertoire: each master's unique skillset in general
  form, the dispatch table (which lens, when), and the boundary lessons where each skillset
  failed its own owner. Triggered by `/masters` or loaded when a stall matches a lens.
